{
  "name": "canvas-presence-only",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "esbuild app.js --bundle --outfile=static/app.js && node -r dotenv/config index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Guillaume Salles",
  "license": "Apache-2.0",
  "dependencies": {
    "@liveblocks/client": "^0.9.0",
    "@liveblocks/node": "^0.3.0",
    "express": "^4.17.1",
    "nanoid": "^3.1.23"
  },
  "devDependencies": {
    "dotenv": "^10.0.0",
    "esbuild": "^0.12.15"
  }
}
